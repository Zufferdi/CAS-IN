{
  "name": "cas-in-tests",
  "version": "0.0.1",
  "private": true,
  "description": "Smoke tests Playwright pour CAS-IN. Le produit reste vanilla HTML/CSS/JS — ce fichier n'affecte pas le déploiement GitHub Pages, il sert uniquement au dev et à la CI.",
  "scripts": {
    "test":           "playwright test",
    "test:headed":    "playwright test --headed",
    "test:debug":     "playwright test --debug",
    "test:report":    "playwright show-report",
    "test:install":   "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
