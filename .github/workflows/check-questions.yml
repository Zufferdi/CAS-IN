name: Check questions.json

# Quand déclencher le workflow
on:
  push:
    paths:
      - 'questions.json'
      - 'questions.js'
      - 'check_questions.py'
      - '.github/workflows/check-questions.yml'
  pull_request:
    paths:
      - 'questions.json'
      - 'questions.js'
      - 'check_questions.py'
  workflow_dispatch:  # permet de lancer à la main depuis l'onglet Actions

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Run quality checks
        run: |
          # Le script gère .json ou .js contenant du JSON
          if [ -f questions.json ]; then
            python3 check_questions.py questions.json
          else
            python3 check_questions.py questions.js
          fi

      - name: Upload cleaned version as artifact
        if: success()
        run: |
          if [ -f questions.json ]; then
            python3 check_questions.py questions.json --fix --out questions.cleaned.json
          fi
        continue-on-error: true

      - name: Save cleaned artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: questions-cleaned
          path: questions.cleaned.json
          if-no-files-found: ignore
          retention-days: 30
